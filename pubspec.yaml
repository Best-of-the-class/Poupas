name: pomo
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  go_router: ^17.1.0
  flutter_bloc: ^9.1.1
  font_awesome_flutter: ^11.0.0
  http: ^1.6.0
  get_it: ^9.2.1
  flutter_animate: ^4.5.2
  flutter_dotenv: ^6.0.0
  window_manager: ^0.5.1
  flutter_quill: ^11.5.0
  dotted_border: ^3.1.0
  google_fonts: ^6.2.1
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^10.1.0
  device_info_plus: ^12.4.0
  flutter_localizations:
    sdk: flutter
  ffi: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  flutter_launcher_icons: ^0.13.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - .env


    - lib/core/features/sign_in/presentation/assets/images/poup_caterpillar.png
    - lib/core/features/forgot_password/presentation/assets/images/poup_check.png
    - lib/core/features/user_profile/presentation/assets/images/avatar_1.png
    - lib/core/features/user_profile/presentation/assets/images/avatar_2.png
    - lib/core/features/user_profile/presentation/assets/images/avatar_3.png
    - lib/core/features/user_profile/presentation/assets/images/avatar_4.png
    - lib/core/features/user_profile/presentation/assets/images/avatar_5.png
    - lib/core/features/user_profile/presentation/assets/images/avatar_6.png
    - lib/core/features/user_profile/presentation/assets/images/check.png
    - lib/core/assets/images/folha-icon-active-menu.png
    - lib/core/assets/icons/icon-modulo-iniciante.png
    - lib/core/assets/icons/icon-modulo-intermediario.png
    - lib/core/assets/icons/icon-modulo-avancado.png
    - lib/core/assets/icons/icon-maca-trilha.png
    - lib/core/assets/icons/icon-lagarta-direita-trilha.png
    - lib/core/assets/icons/icon-lagarta-esquerda-trilha.png
    - lib/core/assets/icons/icon-licao-conceito.png
    - lib/core/assets/icons/icon-licao-questao.png
    - lib/core/assets/icons/icon-licao-prova.png
    - lib/core/assets/icons/icon-iniciar-prova-final.png
    - lib/core/assets/icons/icon-poup-circle.png
    - lib/core/assets/icons/icon-btn-dicionario.png
    - lib/core/assets/icons/icon-btn-pratica.png
    - lib/core/assets/images/poup-happy.png
    - lib/core/assets/images/maca-check.png
    - lib/core/assets/images/maca-sad.png
    - lib/core/assets/images/poup_bottom.png    
    - lib/core/assets/images/poupas_assinatura.png
    - lib/core/assets/images/background-arvore-trilha.png
    - lib/core/features/entry/presentation/assets/image/Poup.png
    - lib/core/features/entry/presentation/assets/image/oculos.png
    - lib/core/features/entry/presentation/assets/image/oi.png
    - lib/core/features/badges/presentation/assets/calendar.png
    - lib/core/features/badges/presentation/assets/dollar.png
    - lib/core/features/badges/presentation/assets/guru.png
    - lib/core/features/badges/presentation/assets/lending.png
    - lib/core/features/badges/presentation/assets/megamente.png
    - lib/core/features/badges/presentation/assets/pencil.png
    - lib/core/features/badges/presentation/assets/run.png
    - lib/core/features/badges/presentation/assets/student.png
    - lib/core/features/badges/data/badges.json

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "lib/core/assets/images/poupas_icon.png"
  adaptive_icon_background: "#E03131"
